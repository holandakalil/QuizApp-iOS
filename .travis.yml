os: osx
osx_target: xcode12.5
xcode_sdk: iphonesimulator14.5
language: swift
branches:
  only:
  - main
variables:
  GIT_SUBMODULE_STRATEGY: recursive
script: xcodebuild clean build test -workspace QuizApp.xcworkspace -scheme "CI" 